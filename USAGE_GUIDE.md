# 🎯 Руководство по оптимальному использованию агентов

## Текущая настройка: Изолированный режим ✅

Все агенты настроены с `context: fork`, что означает:
- **Работают в отдельном контексте** (не видят основной чат)
- **Не засоряют историю** основного разговора
- **Возвращают только результат** в основной чат
- **Экономят токены** в основном контексте

## Варианты использования

### 1️⃣ **Рекомендуемый способ: Изолированные агенты**
```bash
# В основном чате просто вызываешь:
/git-helper status

# Агент:
# - Запускается в отдельном процессе
# - Выполняет задачу
# - Возвращает только результат
# - Основной чат остаётся чистым
```

**✅ Плюсы:**
- Чистый основной контекст
- Быстрая работа
- Можно запускать параллельно несколько агентов
- Нет лимита на размер операций

**❌ Минусы:**
- Агент не знает контекст текущей задачи
- Нужно передавать параметры явно

### 2️⃣ **Альтернатива: В основном контексте**
Если нужно, чтобы агент знал контекст, можно создать версии без изоляции:

```bash
# Создать неизолированную версию
/git-helper-context  # Работает в основном чате
```

### 3️⃣ **Отдельные терминалы Claude**
```bash
# Terminal 1 - Основная разработка
claude --model sonnet

# Terminal 2 - Git операции
claude --model haiku
> /git-helper

# Terminal 3 - Code review
claude --model opus
> /pr-review
```

**✅ Плюсы:**
- Полная изоляция
- Разные модели для разных задач
- Параллельная работа

**❌ Минусы:**
- Нет общего контекста между окнами
- Нужно копировать информацию между окнами

## Практические рекомендации

### Для быстрых операций → Изолированные skills
```bash
/linter               # Быстрая проверка
/git-helper status    # Статус репозитория
```

### Для сложных задач → Основной контекст
```bash
# Сначала объясни задачу
"Нужно сделать рефакторинг компонента UserProfile"

# Потом используй агентов
/pr-review   # Они будут знать контекст
```

### Для параллельной работы → Разные терминалы
```bash
# Terminal 1: Разработка
claude --model opus

# Terminal 2: Постоянный мониторинг
claude --model haiku
> while true; do /linter; sleep 300; done
```

## Создание контекстных версий агентов

Если хочешь версии с контекстом, создай дополнительные skills:

```bash
# Копируем существующий skill
cp -r .claude/skills/git-helper .claude/skills/git-helper-ctx

# Редактируем SKILL.md
# Убираем строку "context: fork"
# Меняем name на "git-helper-ctx"
```

## Оптимальный Workflow

### 🚀 Рекомендую такую схему:

1. **Основное окно** - для разработки (model: opus)
   - Пишешь код
   - Решаешь задачи
   - Используешь изолированные skills для быстрых проверок

2. **Фоновые проверки** (опционально)
   - Отдельный терминал с model: haiku
   - Периодический `/linter`
   - Git операции

3. **Code Review** - когда нужно
   - `/pr-review` в изолированном режиме
   - Или отдельное окно для детального review

## Команды для разных сценариев

### Минимальный контекст (экономия токенов):
```bash
/git-helper status        # Изолированно
/linter fix              # Изолированно
/pr-review               # Изолированно
```

### Нужен контекст задачи:
```bash
# Вариант 1: Передай параметры
/git-helper commit "Implemented user authentication"

# Вариант 2: Создай контекстную версию
# (нужно сначала создать git-helper-ctx без context: fork)
/git-helper-ctx
```

### Параллельная работа:
```bash
# В основном чате можно вызвать сразу несколько:
/linter & /git-helper status & /pr-review

# Все выполнятся параллельно в изоляции
```

## FAQ

**Q: Агент не видит мой код/задачу?**
A: Он работает в изоляции. Передай параметры явно или создай версию без `context: fork`

**Q: Хочу, чтобы git-helper знал контекст моей работы**
A: Создай `git-helper-ctx` без изоляции или работай в основном чате

**Q: Можно запустить несколько агентов одновременно?**
A: Да! В изолированном режиме они работают параллельно

**Q: Какой способ самый быстрый?**
A: Изолированные skills с model: haiku для простых операций