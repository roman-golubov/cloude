# Claude Code Skills - Кастомные агенты

## Установленные агенты

В проекте настроены три специализированных агента для упрощения рутинных задач:

### 1. 🔧 Git Helper (`/git-helper`)
Агент для работы с Git операциями.

**Возможности:**
- Управление ветками (создание, переключение, слияние)
- Создание коммитов с правильным форматированием
- Работа со stash
- Очистка репозитория
- Merge и rebase операции

**Использование:**
```bash
# Базовый вызов - агент спросит, что нужно сделать
/git-helper

# Создать коммит
/git-helper commit

# Проверить статус
/git-helper status

# Переключить ветку
/git-helper checkout branch-name
```

### 2. 🔍 PR Review (`/pr-review`)
Агент для code review pull requests. Фокусируется только на frontend коде.

**Возможности:**
- Проверка TypeScript типов
- Анализ React компонентов
- Проверка производительности
- Выявление проблем безопасности
- Проверка соответствия code style

**Использование:**
```bash
# Review текущих изменений против master
/pr-review

# Review конкретного PR (если используется gh cli)
/pr-review 123
```

### 3. ✨ Linter (`/linter`)
Агент для автоматической проверки и исправления качества кода.

**Возможности:**
- Запуск ESLint
- Проверка TypeScript компилятора
- Форматирование через Prettier
- Автоматическое исправление проблем
- Детальный отчёт о проблемах

**Использование:**
```bash
# Проверка inspection-app (по умолчанию)
/linter

# Проверка конкретного приложения
/linter workshop-app

# Автоматическое исправление всех возможных проблем
/linter fix

# Строгая проверка
/linter strict
```

## Как работают агенты

1. **Автоматический вызов**: Claude может автоматически вызвать агента, когда контекст подходит под его описание
2. **Ручной вызов**: Вы можете явно вызвать агента командой `/имя-агента`
3. **С аргументами**: Можно передавать аргументы для конкретных операций

## Настройка и модификация

Файлы конфигурации агентов находятся в:
```
.claude/skills/
├── git-helper/SKILL.md
├── pr-review/SKILL.md
└── linter/SKILL.md
```

### Структура SKILL.md файла:
```yaml
---
name: skill-name              # Имя для вызова через /команду
description: When to use       # Когда Claude должен использовать
allowed-tools:                 # Какие инструменты доступны агенту
  - Bash
  - Read
  - Edit
---

# Markdown инструкции для агента
```

## Полезные команды

```bash
# Посмотреть все доступные skills
/skills

# Обновить или отредактировать skill
# Просто отредактируйте соответствующий SKILL.md файл

# Создать новый skill
mkdir .claude/skills/new-skill
echo "---
name: new-skill
description: Description here
---
Instructions here" > .claude/skills/new-skill/SKILL.md
```

## Примеры workflow

### Полный цикл разработки:
```bash
# 1. Начать новую фичу
/git-helper создай новую ветку для фичи

# 2. Написать код
# ... разработка ...

# 3. Проверить качество
/linter

# 4. Создать коммит
/git-helper commit

# 5. Review перед PR
/pr-review
```

### Быстрая проверка перед коммитом:
```bash
/linter fix      # Исправить всё что можно
/pr-review       # Проверить оставшиеся проблемы
/git-helper      # Создать коммит
```

## Troubleshooting

**Агент не появляется в списке команд:**
- Проверьте, что файл SKILL.md находится в правильной директории
- Убедитесь, что YAML frontmatter корректный
- Перезапустите Claude Code

**Агент не вызывается автоматически:**
- Проверьте поле `description` - оно должно соответствовать контексту
- Можно добавить `disable-model-invocation: false` явно
- Используйте ручной вызов через `/команду`

**Ошибки при выполнении:**
- Проверьте `allowed-tools` - все нужные инструменты должны быть разрешены
- Убедитесь, что пути и команды корректные для вашего проекта